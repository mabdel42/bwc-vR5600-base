---
name: start_cfg_session
pack: vR5600_base
description: Start a configuration session
runner_type: python-script
entry_point: "start_cfg_session.py"
enabled: true
parameters:
    deviceIP:
        type: string
        description: IP address of vRouter
        required: true
    login:
        type: string
        description: Login of vRouter
        required: true
    password:
        type: string
        description: Password of vRouter
        required: true